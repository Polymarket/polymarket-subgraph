const CONDITIONAL_TOKENS = '{{lowercase contracts.ConditionalTokens.address}}';
const USDC = '{{lowercase contracts.USDC.address}}';
const NEG_RISK_WRAPPED_COLLATERAL =
  '{{lowercase contracts.NegRiskWrappedCollateral.address}}';
const NEG_RISK_ADAPTER = '{{lowercase contracts.NegRiskAdapter.address}}';
const EXCHANGE = '{{lowercase contracts.Exchange.address}}';
const NEG_RISK_EXCHANGE = '{{lowercase contracts.NegRiskExchange.address}}';
const NEG_RISK_OPERATOR = '{{lowercase contracts.NegRiskOperator.address}}';

export {
  CONDITIONAL_TOKENS,
  USDC,
  NEG_RISK_WRAPPED_COLLATERAL,
  NEG_RISK_ADAPTER,
  EXCHANGE,
  NEG_RISK_EXCHANGE,
  NEG_RISK_OPERATOR,
};
